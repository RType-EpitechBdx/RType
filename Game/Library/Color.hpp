#pragma once

/** struct Color
 * @brief struct colot to define a color for an entity
 * @struct Color
 * @param r -> red
 * @param g -> green
 * @param b -> blue
 * @param a -> alpha
 */

struct Color {
    int r;
    int g;
    int b;
    int a;
};